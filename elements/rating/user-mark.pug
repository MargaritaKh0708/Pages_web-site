include ../circle/circle


mixin user-mark(mark)
    .user-mark
        if mark==5
            -for (let x=0; x < mark; x++)
                +circle&attributes(attributes) 
        else if mark==4.5
            +circle&attributes(attributes)
            +circle&attributes(attributes)
            +circle&attributes(attributes)
            +circle&attributes(attributes)
            +circle('half')&attributes(attributes)
        else
            +circle&attributes(attributes) 
            +circle&attributes(attributes)
            +circle&attributes(attributes)
            +circle&attributes(attributes)
            +circle('empty')&attributes(attributes)


        //- if mark==5
        //-     -for (var x=0; x < mark; x++)
        //-         +circle 
        //- else if mark>4 & mark<5
        //-     -for (var y=0; y < 4; x++)
        //-         +circle 
        //-     +circle('half')
        //- else
        //-     -for (var z=0; z < 4; x++)
        //-         +circle 
        //-     +circle('empty')